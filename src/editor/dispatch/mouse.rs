use super::Editor;

impl Editor {
	pub(crate) fn cmd_mouse_down(&mut self, _col: u16, _row: u16) {
		// Filled in by Task 4 (hit-test + cursor placement).
	}

	pub(crate) fn cmd_mouse_drag(&mut self, _col: u16, _row: u16) {
		// Filled in by Task 4.
	}

	pub(crate) fn cmd_mouse_up(&mut self, _col: u16, _row: u16) {
		// Filled in by Task 4.
	}
}
